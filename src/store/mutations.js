export const SET_AUTH = (state, auth) => {
  state.auth = auth;
};

export const SET_LEADS = (state, leads) => {
  state.leads = leads;
};
export const TOTAL_LEADS = (state, total) => {
  state.totalLeads = total;
};
export const SINGLE_LEAD = (state, lead) => {
  state.lead = lead;
};
